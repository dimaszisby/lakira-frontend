import MetricFormDialog from "@/features/metrics/components/MetricFormDialog";

const CategoryMetricCreatePage = () => {
  return <MetricFormDialog initialMetric={null} />;
};

export default CategoryMetricCreatePage;
