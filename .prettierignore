.next
node_modules
dist
coverage
out

# Generated — Prettier must not touch these, or the codegen drift gates
# (npm run api:types:check / api:spec:check) fail against a reformatted file.
src/types/api/generated
docs/reference/api/lakira-backend-openapi.json
src/styles/output.css

# Agent-owned. .claude/hooks/format-on-edit.sh already skips these; keeping
# `npm run format:fix` out too closes the same hole (the backend hit a bug
# where the formatter re-injected frontmatter into agent-memory files).
.claude

# Structured records. `.claude/rules/documentation.md` says never to reformat
# or condense these; the rule was stated but not enforced, so the formatter
# still wanted to rewrite all seven. Ignoring them makes the rule mechanical.
# Incidents are deliberately NOT here — see that rule's scope.
docs/internal/audits
